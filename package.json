{
  "name": "tail-grep-viewer",
  "displayName": "Tail + Grep Viewer",
  "description": "Read-only tail -f style viewer with live follow, inline grep-style filtering, and a safe low-level file clear (truncate) command.",
  "version": "0.1.0",
  "publisher": "joshuacarter",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Other"],
  "activationEvents": [
    "onCommand:tailGrepViewer.open"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tailGrepViewer.open",
        "title": "Tail + Grep: Open File",
        "category": "Tail Viewer",
        "icon": "$(eye)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "tailGrepViewer.open",
          "when": "!explorerResourceIsFolder",
          "group": "navigation@100"
        }
      ],
      "editor/title/context": [
        {
          "command": "tailGrepViewer.open",
          "group": "navigation@100"
        }
      ],
      "editor/context": [
        {
          "command": "tailGrepViewer.open",
          "group": "navigation@100"
        }
      ],
      "commandPalette": [
        {
          "command": "tailGrepViewer.open"
        }
      ]
    }
  }
}
